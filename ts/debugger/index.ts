export { FrameStats } from "./FrameStats";

export const DEBUGGER_DISPLAY_ELEMENT_SELECTOR = "#debug-output"
export const CONSOLE_LOG_RENDER_TIME_THRESHOLD = 200; //milliseconds, anything that takes more that to render is logged set to 0 to log everything
